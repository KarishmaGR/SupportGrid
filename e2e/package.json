{
  "name": "@supportgrid/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test --pass-with-no-tests",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "dependencies": {
    "better-auth": "*",
    "pg": "*"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "dotenv": "^16.0.0"
  }
}
